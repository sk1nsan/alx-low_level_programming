#include <stdlib.h>
#include <time.h>
#include <stdio.h>

int add(int, int);

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	return (0);
}

/**
 * add - adds 2 numbers
 *
 * Return: int a+b
 */

int add(int a, int b)
{
	return (a + b);
}
